pipeline {
    agent any
    environment {
        PANDOC_IMAGE = 'pandoc/latex'
    }

    stages {
        stage('Run Pandoc') {
            steps {
                script {
                    sh 'pwd'
                    sh 'ls'
                    sh "docker run --rm -v ${pwd()}:/workspace -w /workspace ${PANDOC_IMAGE} README.md -o output.html"
                    sh "cat output.html"
                }
            }
        }
    }
}
